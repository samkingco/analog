import React from "react";
import Svg, { SvgProps } from "react-native-svg";

export const BlankIcon = (props: SvgProps) => (
  <Svg width={24} height={24} viewBox="0 0 24 24" fill="none" {...props} />
);
